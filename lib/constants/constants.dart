class ApiConstants {
  // Create these variables as static so they can be called without creating an instance of the class
  static String baseUrl = 'https://jsonplaceholder.typicode.com/';
  static String usersEndPoint = '/users';
}
